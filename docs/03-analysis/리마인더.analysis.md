# 리마인더 Gap Analysis Report

**분석일**: 2026-03-21
**Match Rate**: 78%
**Status**: ⚠️ Act phase 필요 (< 90%)

---

## 1. 분석 요약

| 카테고리 | 점수 | 상태 |
|---------|:---:|:---:|
| 기능 구현 (F-01~F-09) | 78% | Warning |
| 아키텍처 준수 | 90% | OK |
| 컨벤션 준수 | 88% | OK |
| **Overall** | **78%** | **Warning** |

---

## 2. 기능별 구현 현황

| ID | 기능 | 상태 | 구현 위치 | 비고 |
|----|------|:---:|---------|-----|
| F-01 | 시간 표현 감지 | ✅ OK | `src/lib/parseReminderIntent.ts` | 오늘/내일/모레, 요일, N월N일, 시/분 + 키워드 감지 |
| F-02 | "알림 설정해드릴까요?" 제안 | ✅ OK | `src/components/ReminderPrompt.tsx` | [알림 설정]/[괜찮아요] 플로팅 카드 |
| F-03 | 사용자 동의 처리 | ⚠️ Partial | `src/components/ReminderPrompt.tsx` | 버튼 클릭만 구현 — "응"/"네" 텍스트 입력 동의 미구현 |
| F-04 | FCM 토큰 취득 | ✅ OK | `src/lib/firebase.ts` | `requestFCMToken()` + VAPID key |
| F-05 | reminders 테이블 INSERT | ✅ OK | `src/lib/reminders.ts` | `addReminder()` 모든 컬럼 매핑 |
| F-06 | 브라우저 Notification 권한 | ✅ OK | `src/lib/firebase.ts:29` | `Notification.requestPermission()` |
| F-07 | PWA SW 푸시 수신 | ✅ OK | `public/firebase-messaging-sw.js` | 백그라운드 + 포그라운드 처리 |
| F-08 | 캘린더 알림 설정 | ⚠️ Partial | `src/components/calendar/ReminderSheet.tsx` | EventForm 확장 아닌 독립 폼 — 기능은 동작하나 설계 의도와 다름 |
| F-09 | 알림 발송 트리거 | ❌ Missing | 없음 | Edge Function / Vercel Cron 미구현 — 등록만 되고 실제 발송 불가 |

**점수 계산**: 6 Full(×1) + 2 Partial(×0.5) + 1 Missing(×0) = **7/9 ≈ 78%**

---

## 3. Gap 목록

### Critical

| Gap | 설명 | 영향 |
|-----|------|------|
| F-09 알림 발송 트리거 미구현 | Supabase Edge Function `send-reminder` 또는 Vercel Cron `/api/cron/send-reminders` 없음 | **Critical** — reminders 테이블에 등록은 되나 실제 FCM push 미발송 |

### Medium

| Gap | 설명 | 영향 |
|-----|------|------|
| F-03 텍스트 동의 미구현 | "응"/"네"/"설정해줘" 채팅 입력 감지 없음 — 버튼 클릭만 가능 | Medium — Plan 핵심 UX 흐름 누락 |
| F-08 EventForm 확장 아닌 독립 폼 | Plan: EventForm에 "알림 N분 전" 필드 추가 / 실제: 별도 ReminderSheet | Medium — UX 흐름 다름 |

### Low

| Gap | 설명 |
|-----|------|
| `useReminder.ts` 훅 파일 미존재 | Plan 구현순서 #7 명시 — chat/layout.tsx가 대체하므로 기능상 동일 |
| 일정 삭제 시 연관 reminder 삭제 미구현 | cascade 없음 |
| `.env.example` 미생성 | Firebase 환경변수 8개 템플릿 없음 |

---

## 4. 추가 구현된 항목 (Plan에 없음)

| 항목 | 위치 | 설명 |
|------|------|------|
| `listenForegroundMessages()` | `src/lib/firebase.ts` | 포그라운드 메시지 리스너 |
| 캘린더 FAB 버튼 | `src/app/calendar/layout.tsx` | 우하단 플로팅 알림 추가 버튼 |
| 구형 SW 제거 로직 | `src/lib/firebase.ts` | Serwist SW 충돌 방지 unregister |

---

## 5. 아키텍처 준수 (90%)

- "기존 코드 무변경" 원칙 ✅ — chat/page.tsx, calendar/page.tsx 미수정, layout.tsx 주입 방식
- 의존성 방향 정상: Components → lib (firebase, reminders, parseReminderIntent)
- KST → UTC 변환 후 저장 ✅

---

## 6. 권장 조치

### 즉시 (Critical)
1. **F-09 알림 발송 트리거** 구현 — Vercel Cron `/api/cron/send-reminders` 또는 Supabase Edge Function

### 단기
2. **F-03 텍스트 동의** — `chat/layout.tsx`에서 "응"/"네"/"설정해줘" 입력 감지 후 자동 confirm
3. **`.env.example`** 생성 — Firebase 환경변수 8개 템플릿
